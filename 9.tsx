import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faUser } from '@fortawesome/free-solid-svg-icons';

const MyComponent = () => (
  <div>
    <FontAwesomeIcon icon={faUser} /> {/* 例：ユーザーアイコン */}
  </div>
);
/*ユーザーアイコンを表示でいるようにする */